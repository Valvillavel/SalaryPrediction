[pytest]
minversion = 8.0
addopts = -ra -q --strict-markers --cov=src --cov-report=term-missing
testpaths = tests
pythonpath = .
markers =
    slow: pruebas lentas (entrenan modelos reales, incluida la red neuronal)
    integration: pruebas de integración de punta a punta