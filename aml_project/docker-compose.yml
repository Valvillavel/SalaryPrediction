services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: salary-api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models:ro
    restart: unless-stopped
